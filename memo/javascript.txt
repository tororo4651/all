<script> の記述位置、属性

スクリプトが動作しない環境用の情報を明示する 書き方、記述場所

規定の動作を停止する（ <a> 、クリック ）書き方（ JavaScript、jQuery ）

ライブラリ

スライドダウン・アップ の書き方（ javaScript（２つ）、jQuery（２つ））

デバイスのモードがダークモードならダークモード、ライトモードならライトモードのスタイルにする 書き方（ JavaScript ）

JavaScript で、IE なら、処理を実行する 書き方

"条件演算子" の別名、書き方

画面の横幅を取得する書き方（ JavaScript、jQuery ）、違い

配列を代入する変数名・定数名 は、慣習上、？ にすることが多い。

値の型を返す 書き方、値の型を ？ で返す、数値・文字列・論理値・配列・オブジェクトの値の型、if文での書き方

"可変長引数" の読み方、別名・読み方、書き方（ 関数名：getFullName ）

"引数の展開" の書き方、関数名 getFullName の書き方



const cat = {
  name: 'タマ',
  age: 3,
  color: 'black'
}

オブジェクトの name を参照する 書き方（２つ）



const user = {
  name: '山田 太郎',
  age: 28,
  hobby: '旅行'
};

userオブジェクトのプロパティ名・値を、繰り返し処理で取り出す 書き方


for (const key in user) {
  console.log(key, user[key]);
}



１番優先順位が高い演算子は？

switch文 の書き方、== と === のどちらで判定されているか？

"クラス" の書き方（ ゲッタ、セッタ、継承、オーバーライド ）

Mapオブジェクト の書き方（ list ）

Setオブジェクト の書き方（ list ）

forEach()、every()、some()、map()、filter()、find()、findIndex、reduce() の書き方
